# ZeroShot-Object-Detection-
Zero-Shot Object Detection and Tracking System using GroundingDINO, SAM, BLIP, and DeepSORT. The pipeline detects objects from text prompts, generates segmentation masks, produces image captions, and performs multi-object tracking across frames.
